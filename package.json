{
  "name": "livetruth",
  "version": "1.0.0",
  "description": "Real-Time Fact Checker for LIVE Speech",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && node index.js",
    "client": "cd client && npm run dev",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "install-server": "cd server && npm install",
    "install-client": "cd client && npm install",
    "clean-install": "npm cache clean --force && npm install && cd server && npm cache clean --force && npm install && cd ../client && npm cache clean --force && npm install",
    "postinstall": "echo Installation complete! Please set up .env files before running."
  },
  "keywords": ["fact-checking", "speech", "ai", "ml"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.2"
  }
}
